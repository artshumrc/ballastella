{
	"name": "ballastella",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"license": "MIT",
	"packageManager": "pnpm@11.18.0",
	"engines": {
		"node": ">=22"
	},
	"scripts": {
		"build": "pnpm -r build",
		"test": "pnpm -r test && pnpm run test:scripts",
		"test:scripts": "node --test scripts/*.test.mjs",
		"test:e2e": "playwright install chromium && playwright test",
		"flake:check": "node scripts/flake-check.mjs",
		"check": "tsc --noEmit && pnpm -r check",
		"lint": "node scripts/check-viewer-deps.mjs && node scripts/check-tiler-lazy.mjs && node scripts/check-base-map-catalog.mjs && node scripts/check-workspace-rooted-paths.mjs && node scripts/check-alignment-writers.mjs && node scripts/check-allmaps-patch.mjs && prettier --check . && eslint .",
		"check:bundles": "node scripts/check-tiler-lazy.mjs --require-build",
		"check:deployment": "node scripts/check-base-map-catalog.mjs --deployment",
		"check:dev": "node scripts/check-dev-boot.mjs",
		"format": "prettier --write ."
	},
	"devDependencies": {
		"@allmaps/annotation": "catalog:",
		"@eslint/js": "^10.0.1",
		"@playwright/test": "^1.62.1",
		"@types/node": "^26.1.2",
		"eslint": "^10.8.0",
		"eslint-config-prettier": "^10.1.8",
		"eslint-plugin-svelte": "^3.22.0",
		"fflate": "catalog:",
		"globals": "^17.9.0",
		"pmtiles": "catalog:",
		"prettier": "^3.9.6",
		"prettier-plugin-svelte": "^4.1.1",
		"prettier-plugin-tailwindcss": "^0.8.1",
		"svelte": "catalog:",
		"typescript": "catalog:",
		"typescript-eslint": "^8.66.0"
	}
}
